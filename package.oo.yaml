version: 1.0.0
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: pip install pandas
  bootstrap: |
    npm install
    pip install -r requirements.txt
dependencies:
  {}
name: qrcode
description: QR Code Reader / Generator
displayName: QR Code
repository: https://github.com/BlackHole1/oomol-qrcode
releaseNotes: First release
icon: ":fa6-solid:qrcode:#32C5FF:"
